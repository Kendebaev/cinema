global:
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'cinema_app'
    static_configs:
      - targets: ['app:3000'] # Твоё Node.js приложение

  - job_name: 'node_exporter'
    static_configs:
      - targets: ['node-exporter:9100'] # Метрики самого сервера (CPU/RAM)